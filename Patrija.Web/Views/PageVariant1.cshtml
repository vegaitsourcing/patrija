@inherits UmbracoViewPage<PageVariant1ViewModel>
}

@if (Model.TextBlockViewModel != null)
{
    Html.RenderAction<NestedContentController>(c => nameof(c.TextBlock), new { viewModel = Model.TextBlockViewModel });
}

@if (Model.ImageWithText != null)
{
    Html.RenderAction<NestedContentController>(c => nameof(c.ImageWithText), new { viewModel = Model.ImageWithText });
}
