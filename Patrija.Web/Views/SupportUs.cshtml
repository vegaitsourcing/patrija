@inherits Umbraco.Web.Mvc.UmbracoViewPage<SupportUsViewModel>

@if (Model.DonationForm != null)
{
    Html.RenderAction<NestedContentController>(c => nameof(c.SimpleForm), new { viewModel = Model.DonationForm });
}